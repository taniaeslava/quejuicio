# Dependencias de Node (solo se instalan en GitHub Actions, no en el repo)
node_modules/

# Proyecto Android que Capacitor genera en la nube (no se versiona)
android-app/android/
android-app/QueJuicio.apk

# Claves de cuenta de servicio — NUNCA subir al repo (van como secreto)
*serviceAccount*.json
*service-account*.json
